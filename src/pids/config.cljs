(ns pids.config)
  (def streamtype "replay")
  (def mvBase "http://mv-dev.realeyes.cloud/?")
(def showms false)
(def showdisco false)
(def pidrange (into [] (concat ;(range 28500 31500)
                        (range 202000 210000)
                        ;(range 400000 404000)
                        )))
