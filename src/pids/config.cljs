(ns pids.config)
  (def streamtype "live")
(def mvBase "http://mv.realeyes.cloud/?")
(def showms false)
(def showdisco false)
(def pidrange (into [] (concat ;(range 28500 31500)
                        (range 200000 203000)
                        ;(range 400000 404000)
                        )))
