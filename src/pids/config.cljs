(ns pids.config)
  (def streamtype "replay")
  (def pidrange (into [] (concat ;(range 28500 31500)
                        ;(range 200000 201000)
                        ;(range 202700 204000)
                        (range 202000 205000))))

